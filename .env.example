# 이 파일을 복사해서 .env로 이름 바꾸고, 실제 값을 채워넣으세요.
# .env는 절대 git에 커밋하지 마세요.

# ★ Access Key/Secret Key는 여기 안 넣습니다 (학교 정책상 발급 자체가 불가).
#   대신 API Gateway 주소만 넣으면, 그 뒤의 Lambda(IAM Role)가 인증을 대신 처리합니다.

# API Gateway invoke URL (배포 후 발급됨)
API_GATEWAY_URL=

# 비워두면 로컬 테스트 모드로 동작 (AWS 없이 로컬 네트워크 QR로 테스트 가능)
